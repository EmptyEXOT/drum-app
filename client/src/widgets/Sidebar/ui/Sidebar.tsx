import React from 'react';
import classNames from "classnames";

export const Sidebar = () => {
    return (
        <div className={classNames('w-[20vw] h-screen bg-amber-50')}>
            menu
        </div>
    );
};
